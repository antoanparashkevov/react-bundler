import React from "react";

const ServerPage: React.FC = (): React.ReactNode => {
    return (
        <section>
            <h1 className='text-white font-bold'>
                Welcome from React + Tailwind! :)
            </h1>
        </section>
    )
}

export default ServerPage;